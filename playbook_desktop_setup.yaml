
---
- name: Desktop PC setup playbook
  hosts: localhost
  remote_user: leon
  become: true
  gather_facts: true

  roles:
    - ../roles/variable_definitions
    - ../roles/desktop
    - ../roles/nas-storage
